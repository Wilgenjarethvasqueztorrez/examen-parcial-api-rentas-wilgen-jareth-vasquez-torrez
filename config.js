const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost",
      user: "root",
      password: "wilgen12345",
      database: "casas",
    },
    listPerPage: 10,
  };
  module.exports = config;